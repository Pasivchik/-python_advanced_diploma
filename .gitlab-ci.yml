image: python:3.11-slim

stages:
  - test

simple-test:
  stage: test
  script:
    - echo "Starting pipeline..."
    - python --version
    - pip install pytest
    - python -m pytest --version
    - echo "Pipeline completed!"
  only:
    - merge_requests
    - main