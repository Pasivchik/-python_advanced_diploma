# 🐦 Twitter Clone API

Микросервис социальной сети, реализующий основные функции Twitter: публикация твитов, лайки, подписки и работа с медиафайлами.

---

## 📋 Содержание

- [🛠️ Технологии](#технологии)
- [📦 Требования](#требования)
- [🚀 Установка и запуск](#установка-и-запуск)
- [📁 Структура проекта](#структура-проекта)
- [📖 API документация](#api-документация)
- [💡 Примеры использования](#примеры-использования)
- [👨‍💻 Разработка](#разработка)
- [🔧 Troubleshooting](#troubleshooting)

---

## 🛠️ Технологии

- **Backend**: Flask 3.1.2, Flask-SQLAlchemy 3.1.1
- **База данных**: PostgreSQL 15
- **ORM**: SQLAlchemy
- **API документация**: Flasgger (Swagger UI)
- **Контейнеризация**: Docker, Docker Compose
- **Python**: 3.11

---

## 📦 Требования

- Docker версии 20.10 или выше
- Docker Compose версии 2.0 или выше
- 2 GB свободного места на диске
- Порты 5000 и 5432 должны быть свободны

---

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
git clone https://gitlab.skillbox.ru/maksim_lazebnyi/python_advanced_diploma
cd twitter-clone
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` файл:

```env
DB_HOST=postgres
DB_PORT=5432
DB_NAME=twitter_database
DB_USER=developer
DB_PASSWORD=your_secure_password
```

### 3. Запуск приложения

```bash
# Сборка и запуск контейнеров
docker-compose up --build

# Или в фоновом режиме
docker-compose up -d --build
```

Приложение будет доступно по адресу:
- **API**: http://localhost:5000
- **Swagger документация**: http://localhost:5000/api/docs/

### 4. Остановка приложения

```bash
# Остановка контейнеров
docker-compose down

# Остановка с удалением volumes (удалит все данные из БД)
docker-compose down -v
```

---

## 📁 Структура проекта

```
.
├── app/
│   ├── __init__.py
│   ├── __main__.py
│   ├── models.py          # Модели базы данных
│   ├── routers.py         # API эндпоинты
│   ├── static/
│   │   ├── css/
│   │   ├── js/
│   │   └── media/         # Загруженные медиафайлы
│   └── templates/
│       └── index.html
├── docker-compose.yml
├── Dockerfile
├── requirements.txt
├── requirements-dev.txt
├── run.py
├── .env.example
└── README.md
```

## API документация

После запуска приложения интерактивная документация Swagger доступна по адресу: http://localhost:5000/api/docs/

### Авторизация

Большинство эндпоинтов требуют авторизации через API ключ, который передается в заголовке `API_KEY`.

По умолчанию создаются два тестовых пользователя:
- **Пользователь 1**: API_KEY = `test`
- **Пользователь 2**: API_KEY = `test_two`

### Основные эндпоинты

#### Твиты

| Метод | Endpoint | Описание | Авторизация |
|-------|----------|----------|-------------|
| GET | `/api/tweets` | Получить все твиты | Нет |
| POST | `/api/tweets` | Создать новый твит | Да |
| DELETE | `/api/tweets/<id>` | Удалить твит | Да |

#### Лайки

| Метод | Endpoint | Описание | Авторизация |
|-------|----------|----------|-------------|
| POST | `/api/tweets/<id>/likes` | Поставить лайк | Да |
| DELETE | `/api/tweets/<id>/likes` | Удалить лайк | Да |

#### Подписки

| Метод | Endpoint | Описание | Авторизация |
|-------|----------|----------|-------------|
| POST | `/api/users/<id>/follow` | Подписаться на пользователя | Да |
| DELETE | `/api/users/<id>/follow` | Отписаться от пользователя | Да |

#### Пользователи

| Метод | Endpoint | Описание | Авторизация |
|-------|----------|----------|-------------|
| GET | `/api/users/me` | Получить информацию о себе | Да |
| GET | `/api/users/<id>` | Получить информацию о пользователе | Нет |

#### Медиафайлы

| Метод | Endpoint | Описание | Авторизация |
|-------|----------|----------|-------------|
| POST | `/api/medias` | Загрузить медиафайл | Нет |
| GET | `/app/static/media/<filename>` | Получить медиафайл | Нет |

## Примеры использования

### 1. Создание твита

```bash
curl -X POST http://localhost:5000/api/tweets \
  -H "Content-Type: application/json" \
  -H "API_KEY: test" \
  -d '{
    "tweet_data": "Мой первый твит!",
    "tweet_media_ids": []
  }'
```

Ответ:
```json
{
  "result": true,
  "tweet_id": 1
}
```

### 2. Создание твита с медиафайлами

Сначала загрузите медиафайл:

```bash
curl -X POST http://localhost:5000/api/medias \
  -F "file=@/path/to/image.jpg"
```

Ответ:
```json
{
  "result": true,
  "media_id": 1
}
```

Затем создайте твит с этим медиафайлом:

```bash
curl -X POST http://localhost:5000/api/tweets \
  -H "Content-Type: application/json" \
  -H "API_KEY: test" \
  -d '{
    "tweet_data": "Твит с картинкой!",
    "tweet_media_ids": [1]
  }'
```

### 3. Получение всех твитов

```bash
curl http://localhost:5000/api/tweets
```

Ответ:
```json
{
  "result": true,
  "tweets": [
    {
      "id": 1,
      "content": "Мой первый твит!",
      "attachments": [],
      "author": {
        "id": 1,
        "name": "test"
      },
      "likes": []
    }
  ]
}
```

### 4. Поставить лайк твиту

```bash
curl -X POST http://localhost:5000/api/tweets/1/likes \
  -H "API_KEY: test_two"
```

### 5. Подписаться на пользователя

```bash
curl -X POST http://localhost:5000/api/users/1/follow \
  -H "API_KEY: test_two"
```

### 6. Получить информацию о себе

```bash
curl http://localhost:5000/api/users/me \
  -H "API_KEY: test"
```

Ответ:
```json
{
  "result": true,
  "user": {
    "id": 1,
    "name": "test",
    "followers": [
      {
        "id": 2,
        "name": "test_two"
      }
    ],
    "following": []
  }
}
```

### 7. Удалить твит

```bash
curl -X DELETE http://localhost:5000/api/tweets/1 \
  -H "API_KEY: test"
```

## Разработка

### Установка зависимостей для разработки

```bash
pip install -r requirements-dev.txt
```

### Локальный запуск без Docker

1. Убедитесь, что PostgreSQL установлен и запущен
2. Создайте базу данных
3. Настройте `.env` файл с локальными параметрами
4. Запустите приложение:

```bash
python run.py
```

### Инструменты разработки

- **Black**: Форматирование кода
- **isort**: Сортировка импортов
- **flake8**: Линтер
- **mypy**: Проверка типов
- **pytest**: Тестирование

```bash
# Форматирование кода
black app/

# Сортировка импортов
isort app/

# Проверка линтером
flake8 app/

# Проверка типов
mypy app/

# Запуск тестов
pytest
```

### Структура базы данных

Приложение использует следующие таблицы:

- **users**: Пользователи
- **tweets**: Твиты
- **likes**: Лайки
- **subscribes**: Подписки
- **media**: Медиафайлы
- **tweet_media**: Связь твитов и медиафайлов (многие-ко-многим)

База данных создается автоматически при первом запуске приложения.

## Troubleshooting

### Порт 5000 уже занят

Измените порт в `docker-compose.yml`:

```yaml
ports:
  - "8000:5000"  # Изменить первое число
```

### Порт 5432 уже занят

Если у вас уже запущен PostgreSQL локально:

```yaml
ports:
  - "5433:5432"  # Изменить первое число
```

И обновите `.env`:
```env
DB_PORT=5433
```

### База данных не подключается

Проверьте логи контейнеров:

```bash
docker-compose logs postgres
docker-compose logs web
```

Убедитесь, что PostgreSQL контейнер здоров:

```bash
docker-compose ps
```

### Ошибка при загрузке медиафайлов

Убедитесь, что директория `app/static/media` существует и имеет права на запись:

```bash
mkdir -p app/static/media
chmod 755 app/static/media
```

### Пересоздание базы данных

Если нужно полностью очистить данные:

```bash
docker-compose down -v
docker-compose up --build
```

⚠️ **Внимание**: Это удалит все данные из базы!

### Просмотр логов

```bash
# Все логи
docker-compose logs

# Логи конкретного сервиса
docker-compose logs web
docker-compose logs postgres

# Следить за логами в реальном времени
docker-compose logs -f web
```

## Поддержка

При возникновении проблем:
1. Проверьте раздел [Troubleshooting](#troubleshooting)
2. Изучите логи: `docker-compose logs`
3. Проверьте Swagger документацию: http://localhost:5000/api/docs/
